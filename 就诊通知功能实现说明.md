# 医院管理系统 - 就诊通知功能实现说明

## 一、功能概述

本次实现针对医院管理系统的就诊通知功能进行了全面优化，主要包括以下改进：

1. **通知触发逻辑修改**：预约成功后一次性生成所有后续提醒通知
2. **通知状态跟踪**：实时监控每条通知的发送状态（成功/失败）
3. **失败处理机制**：自动重试、失败记录、手动处理
4. **管理员后台功能**：通知记录管理、查询筛选、批量操作
5. **数据一致性保障**：通知状态与预约信息保持一致
6. **日志与监控**：完善的日志记录和统计功能

## 二、技术架构

### 后端技术栈
- **框架**：Spring Boot
- **数据库**：MySQL + MyBatis-Plus
- **异步处理**：Spring Async + 线程池
- **定时任务**：Spring Scheduler
- **重试机制**：指数退避算法

### 前端技术栈
- **框架**：Vue 3 + Composition API
- **UI组件库**：Element Plus
- **图表库**：ECharts 5
- **HTTP客户端**：Axios

## 三、后端实现

### 1. 核心实体类

#### TongzhijiluEntity（通知记录实体）
- 位置：`server_code/src/main/java/com/cl/entity/TongzhijiluEntity.java`
- 主要字段：
  - 预约编号、医生账号、用户账号
  - 通知类型（1-预约成功, 2-就诊前1天, 3-就诊前2小时, 4-就诊前15分钟）
  - 发送状态（0-待发送, 1-发送成功, 2-发送失败, 3-发送中）
  - 重试次数、失败原因、下次重试时间
  - 处理状态、处理人、处理备注

### 2. 数据访问层

#### TongzhijiluDao（通知记录DAO）
- 位置：`server_code/src/main/java/com/cl/dao/TongzhijiluDao.java`
- 主要方法：
  - `selectPendingNotifications`：查询待发送通知
  - `selectRetryNotifications`：查询需要重试的通知
  - `selectStatusStatistics`：统计各状态数量
  - `batchUpdateStatus`：批量更新状态

### 3. 业务逻辑层

#### TongzhijiluServiceImpl（通知服务实现）
- 位置：`server_code/src/main/java/com/cl/service/impl/TongzhijiluServiceImpl.java`
- 核心功能：

**通知创建逻辑**：
```java
public void createAppointmentNotifications(...) {
    // 1. 预约成功通知 - 立即发送
    // 2. 就诊前1天提醒
    // 3. 就诊前2小时提醒
    // 4. 就诊前15分钟提醒
}
```

**重试机制**（指数退避算法）：
```java
private void handleSendFailure(TongzhijiluEntity notification, String errorMsg) {
    int retryCount = notification.getChongshicishu() + 1;
    if (retryCount >= maxRetryCount) {
        // 标记为最终失败
    } else {
        // 计算下次重试时间：baseRetryInterval * 2^(retryCount-1)
        int delayMinutes = baseRetryInterval * (int) Math.pow(2, retryCount - 1);
    }
}
```

### 4. 配置类

#### AsyncConfig（异步配置）
- 位置：`server_code/src/main/java/com/cl/config/AsyncConfig.java`
- 配置参数：
  - corePoolSize: 10（核心线程数）
  - maxPoolSize: 50（最大线程数）
  - queueCapacity: 200（队列容量）
  - keepAliveSeconds: 60（线程存活时间）

#### ScheduledConfig（定时任务配置）
- 位置：`server_code/src/main/java/com/cl/config/ScheduledConfig.java`
- 定时任务：
  - 每分钟执行：处理待发送通知
  - 每5分钟执行：处理需要重试的通知
  - 每天凌晨2点：清理过期记录

### 5. 控制器

#### TongzhijiluController（通知记录管理）
- 位置：`server_code/src/main/java/com/cl/controller/TongzhijiluController.java`
- 主要接口：
  - `GET /tongzhijilu/page`：分页查询
  - `GET /tongzhijilu/info/{id}`：获取详情
  - `GET /tongzhijilu/manualRetry/{id}`：手动重试
  - `POST /tongzhijilu/batchRetry`：批量重试
  - `GET /tongzhijilu/markAsProcessed/{id}`：标记已处理
  - `GET /tongzhijilu/statistics`：获取统计
  - `GET /tongzhijilu/exportFailed`：导出失败记录
  - `GET /tongzhijilu/triggerBatchSend`：触发批量发送
  - `GET /tongzhijilu/triggerBatchRetry`：触发批量重试

## 四、前端实现

### 1. API服务层

#### tongzhijilu.js
- 位置：`manage_code/src/api/tongzhijilu.js`
- 提供所有后端接口的封装调用
- 包含状态映射常量（通知类型、发送状态、处理状态、用户类型）

### 2. 页面组件

#### list.vue（通知记录列表页）
- 位置：`manage_code/src/views/tongzhijilu/list.vue`
- 功能特性：
  - 多条件搜索筛选
  - 批量操作（批量重试、批量标记）
  - 数据导出（CSV格式）
  - 分页展示
  - 响应式布局

#### StatisticsPanel.vue（统计面板）
- 位置：`manage_code/src/views/tongzhijilu/components/StatisticsPanel.vue`
- 功能特性：
  - 6个统计卡片展示
  - 发送状态分布饼图
  - 通知类型分布柱状图
  - 点击筛选功能

#### DetailDialog.vue（详情对话框）
- 位置：`manage_code/src/views/tongzhijilu/components/DetailDialog.vue`
- 功能特性：
  - 完整信息展示
  - 处理时间线
  - 快捷操作按钮

## 五、数据库设计

### 通知记录表（tongzhijilu）

```sql
CREATE TABLE `tongzhijilu` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `yuyuebianhao` varchar(200) DEFAULT NULL COMMENT '预约编号',
  `yishengzhanghao` varchar(200) DEFAULT NULL COMMENT '医生账号',
  `zhanghao` varchar(200) DEFAULT NULL COMMENT '用户账号',
  `yonghuleixing` int(11) DEFAULT '1' COMMENT '用户类型(1-用户, 2-医生)',
  `tongzhileixing` int(11) DEFAULT NULL COMMENT '通知类型',
  `tongzhineirong` text COMMENT '通知内容',
  `fasongzhuangtai` int(11) DEFAULT '0' COMMENT '发送状态',
  `jihuafasongshijian` datetime DEFAULT NULL COMMENT '计划发送时间',
  `shijifasongshijian` datetime DEFAULT NULL COMMENT '实际发送时间',
  `shibaiyuanyin` varchar(500) DEFAULT NULL COMMENT '失败原因',
  `chongshicishu` int(11) DEFAULT '0' COMMENT '重试次数',
  `xiacichongshishijian` datetime DEFAULT NULL COMMENT '下次重试时间',
  `jieshourenshouji` varchar(200) DEFAULT NULL COMMENT '接收手机号',
  `chulizhuangtai` int(11) DEFAULT '0' COMMENT '处理状态',
  `chuliren` varchar(200) DEFAULT NULL COMMENT '处理人',
  `chulishijian` datetime DEFAULT NULL COMMENT '处理时间',
  `chulibeizhu` varchar(500) DEFAULT NULL COMMENT '处理备注',
  `jiuzhenshijian` datetime DEFAULT NULL COMMENT '就诊时间',
  `addtime` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_yuyuebianhao` (`yuyuebianhao`),
  KEY `idx_fasongzhuangtai` (`fasongzhuangtai`),
  KEY `idx_jihuafasongshijian` (`jihuafasongshijian`),
  KEY `idx_chulizhuangtai` (`chulizhuangtai`),
  KEY `idx_zhanghao` (`zhanghao`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知记录表';
```

## 六、配置参数

在 `application.yml` 中添加以下配置：

```yaml
# 通知重试配置
notification:
  retry:
    max-count: 3          # 最大重试次数
    base-interval: 5      # 基础重试间隔（分钟）

# 异步线程池配置
async:
  notification:
    core-pool-size: 10    # 核心线程数
    max-pool-size: 50     # 最大线程数
    queue-capacity: 200   # 队列容量
    keep-alive-seconds: 60 # 线程存活时间
```

## 七、使用说明

### 1. 数据库初始化
执行SQL脚本：`server_code/sql/tongzhijilu_table.sql`

### 2. 集成到预约流程
在预约成功后的代码中调用：
```java
@Autowired
private TongzhijiluService tongzhijiluService;

// 预约成功后创建通知记录
tongzhijiluService.createAppointmentNotifications(
    yuyueId, yuyuebianhao, yishengzhanghao, 
    zhanghao, shouji, yuyueshijian
);
```

### 3. 前端访问
登录管理系统后，从菜单进入"通知记录"页面进行管理。

## 八、测试

### 单元测试
- 服务层测试：`TongzhijiluServiceImplTest.java`
- 控制器测试：`TongzhijiluControllerTest.java`

运行测试：
```bash
cd server_code
mvn test
```

## 九、性能优化

1. **异步处理**：使用线程池异步发送通知，避免阻塞主线程
2. **批量操作**：支持批量重试和批量标记，提高效率
3. **数据库索引**：关键字段建立索引，优化查询性能
4. **定时任务**：合理设置定时任务频率，平衡实时性和系统负载
5. **指数退避**：重试间隔指数增长，避免对系统造成压力

## 十、注意事项

1. 实际项目中需要替换 `doSendNotification` 方法中的模拟发送逻辑，接入真实的短信网关或推送服务
2. 建议配置监控告警，当失败率超过阈值时及时通知管理员
3. 定期清理历史数据，避免数据量过大影响性能
4. 生产环境建议配置独立的通知服务，与主业务解耦
